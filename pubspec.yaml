name: cotizaciones_app
description: A new Flutter project.

publish_to: "none" # Remove this line if you plan to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.6.0
  printing: ^5.5.0
  flutter_pdfview: ^1.0.0
  cupertino_icons: ^1.0.8
  path_provider: ^2.0.11
  sqflite: ^2.0.0+4
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/plantilla_cotizacion.pdf #plantilla en formato pdf 
